{
  "name": "default",
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "database": "marketplace",
  "synchronize": true,
  "logging": ["warn", "error"],
  "autoReconnect": true,
  "reconnectTries": 5,
  "reconnectInterval": 3000,
  "entities": [
    "src/entities/*.{js,ts}" 
  ],
  "migrations": [
   "src/migrations/*.{js,ts}"
  ]

}
